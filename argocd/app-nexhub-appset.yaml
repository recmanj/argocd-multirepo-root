apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: app-nexhub
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  goTemplate: true
  goTemplateOptions:
    - "missingkey=error"
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    - matrix:
        generators:
          - clusters:
              selector:
                matchExpressions:
                  - key: env
                    operator: Exists
              values:
                env: '{{ index .metadata.labels "env" }}'
          - git:
              repoURL: git@github.com:recmanj/argocd-multirepo-nexhub.git
              revision: HEAD
              directories:
                - path: "envs/{{ .values.env }}"
  template:
    metadata:
      name: "nexhub"
    spec:
      project: nexhub
      source:
        repoURL: git@github.com:recmanj/argocd-multirepo-nexhub.git
        targetRevision: HEAD
        path: "envs/{{ .values.env }}"
      destination:
        server: "{{ .server }}"
        namespace: "nexhub"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 3
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m

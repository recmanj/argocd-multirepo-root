apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: demo-app-kustomize
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  description: Team B — deploys the demo app via Kustomize. AppSet source is the private config repo (demo-app-kustomize-config); its env overlays pull the public base (demo-app-kustomize) via kustomize remote git URL and pin image tags via versions.yaml.
  sourceRepos:
    - https://github.com/recmanj/argocd-multirepo-demo-app-kustomize.git
    - https://github.com/recmanj/argocd-multirepo-demo-app-kustomize-config.git
  destinations:
    - namespace: demo-app
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: ""
      kind: Namespace
  namespaceResourceWhitelist:
    - group: ""
      kind: ServiceAccount
    - group: ""
      kind: Service
    - group: ""
      kind: ConfigMap
    - group: ""
      kind: Secret
    - group: apps
      kind: Deployment
    - group: apps
      kind: ReplicaSet
    - group: ""
      kind: Pod
    - group: networking.k8s.io
      kind: Ingress
    - group: batch
      kind: Job
    - group: batch
      kind: CronJob
    - group: bitnami.com
      kind: SealedSecret
